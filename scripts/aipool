#!/usr/bin/env bash
set -euo pipefail

# Repository-local launcher: works before the package is installed.
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$ROOT/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m aipool.cli "$@"
